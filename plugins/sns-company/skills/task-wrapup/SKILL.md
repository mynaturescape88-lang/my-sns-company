---
name: task-wrapup
description: >
  タスクのまとめ・クローズ・セッション締め作業を一括実行するときに発動。『まとめ作業して』『クローズして』『締めて』『今日はここまで』等で発動。①学び/FBの昇華(fb/・review-baseline・kb・memory) ②作業記録＝議事メモ(sessionlog)を作成 ③TASKS表更新 ④メディア中間物の棚卸し ⑤選択的コミット&push ⑥報告 を漏れなく順に行う。
---

## 本スキルは下記の手順に従って行うこと

### ① 知識の昇華

- このセッションでオーナーから出たFB・学び・気づき・決定を洗い出す
- `COMMON.md`「FBの消化ライフサイクル」に従い、各々を**分類（Pd/F/R/Wx）→行き先決定→抽象化**する。**分類でMEMORY索引行を作るか否かが分岐する**（下表）：

  | 種類 | 例 | 行き先（正本） | MEMORY正本/索引行 |
  |---|---|---|---|
  | **Pd 手順・方法論** | ショップ記事の書き方、口パクレシピ、TTS手順 | 該当 `.company/knowledge/kb-*.md` ／木を持つ大skillはSKILL本文・`references/*`・`checklist.md` | **作らない** |
  | **F 長期事実** | 各SNSの正体、認証情報の置き場 | memory `project`/`reference` | 残す |
  | **R 参照・定数** | cr単価、APIクォータ、価格 | memory `reference` | 残す |
  | **Wx 横断の働き方** | 承認ゲート、収益最優先、レビュー普遍観点 | `.company/pm/review-baseline.md`（普遍）／そのタスクの `.company/fb/<task>.md`（ドメイン固有の働き方判断基準）に「□〜したか」で1行追記 | **作らない** |

  - **Pd（手順・方法論）の扱い**：Skill本文（kb-*.md／木を持つ大skillの該当ノード）へ直接反映する（そこが正本＝MEMORY正本も索引1行も作らない）。
  - **Wx（横断の働き方）の扱い**：`.company/pm/review-baseline.md`（普遍）／該当タスクの`.company/fb/<task>.md`（ドメイン固有）へ直接反映する（そこが正本＝MEMORY正本も索引1行も作らない）。
  - **F / R**：memory（`project`/`reference`）に正本を保存し、索引行を残す。

- **Pd/Wxをskillへ反映する手順**：
  1. 反映したい内容の「現在の記載」を提示する
  2. 「更新後の記載（案）」を提示する
  3. 更新後の記載（案）は、判断基準が明確な一文にする（曖昧な形容詞を使わない）
  4. 更新後の記載（案）の1行に、2つ以上の要素を含めない
  5. 更新後の記載（案）の書き方に迷ったら、反映先skillの実際の記載を確認してから書く（推測しない）
  6. 更新後の記載（案）について、オーナーの明示的なOKを得る
  7. OK後にskillへ反映する
  8. Pdの場合、反映した手順に**確実に発動させるSkillトリガ語が実在するか**を確認する（トリガが無ければdescription/トリガ語を直す）[[feedback_skill_autotrigger_coverage_fix]]

本手順が実施できない場合：オーナーへ確認する

→ レビューとして、下記がすべてpassになることを確認する
□ セッションの学び・FB・気づきの未分類（Pd手順/F長期事実/R参照/Wx働き方）がゼロ件である
□ Pd/Wxは手順通りに、オーナーにOKをもらってからskillに追加した
□ Pdは反映後、確実に発動させるトリガ語が実在することを確認済みである
□ F/Rはmemoryに正本を保存し、索引行を残したことを確認済みである
□ 既存のMEMORY索引を一括削除していない
□ 実施していないのに「完了」としていないこと

　failした場合：直ちに修正し、再度レビューを実施する
　全passするまで②へ進まない


### ② 作業記録＝議事メモ(sessionlog)を作成（基本形）

このセッションの実務を **議事メモ(sessionlog)** として1ファイル作成する。
再開時はTASKS.md索引＋この最新議事メモを全文読めば続けられる状態にすることが目的

- **命名**＝`yyyyMMddhhmm_<やったことのタイトル>_sessionlog.md`。**時刻はまとめ時に `date` で取得**。
- **格納先**＝`.company/secretary/work/task_logs/YYYY-MM/`（**YYYY-MM＝ファイル名先頭 `yyyyMM`** ＝命名の最初の6桁にハイフン挿入。`date` 再取得に依存せず名前だけで一意に決まる）。**命名規則 `yyyyMMddhhmm_..._sessionlog.md` は不変**（ファイル名は変えずサブフォルダに収めるだけ）。この置き場・命名規約は本スキル②が正本（配置先ディレクトリの地図は `DIRECTORY_TREE.md` §1）。
- **単位**＝1セッション1ファイル。同一セッション内で2回まとめる場合＝**別タスク→別ファイル／同じタスク→既存メモへ追記**。
- **記載ルール**＝全行箇条書き・1行100字以内にすること
- 下記の **基本形テンプレート**（ヘッダ→本体→議事メモ）で作る。「関連ファイル」節は必ず含める（再開時の辿り先）。

```
# yyyyMMddhhmm_<やったことのタイトル>_sessionlog.md
> 親プロジェクト：<プロジェクト名>
> 前セッション＝[<file>](<file>)（凍結・<状態>）／本メモ＝<このセッションの位置づけ>

## ■ ヘッダ（スナップショット）
### 作成日時
- YYYY-MM-DD HH:MM（まとめ時に `date` で取得）
### この作業の背景・目的（なぜやるのか）
- どのような経緯でこのタスクは起票されたのか
- このタスクが何のためか・上位の狙い。単発でも「なぜ今これを」を実施するのか

### 前後の工程（位置づけ）
- 前＝（直前までに何が済んでいるか）／本セッション＝（全体のどこを担ったか）／後＝（この先に続く工程）をストーリーにして記載する

### このセッションでやったこと
- 直近なにをしていたかを時系列順に、1行1実施内容として記載する

### 決定済み事項（前セッション/確定事項）
- このセッションにおける決定事項を、1行1決定事項として記載する

### 未決事項（オーナー確認・判断待ち）
- 残タスクを記載する、1行1タスクで、優先度順に記載する

### 関連ファイル（ありか・再開時はここを辿る）
- 本内容を読み解くために必要な資料を記載する
- <正本/設計/実装/台帳/memory/索引のパスを全部・1行1ファイル＋一言>

## ■ 本体（このセッションの実務・設計内容）
- #議事メモのサマリーとして、結論・要点に絞って記載する
- # このセッションでやったことが漏れなく、実施内容がわかる形で記載されていること
- # 決定済み事項が漏れなく、内容がわかる形で記載されていること
- # 未決事項が漏れなく、内容がわかる形で記載されていること
- オーナーからどんなFB・指摘があったのか内容がわかる形で記載されていること
- 次回着手する内容が決まっている又は別セッションにて作業する場合には、下記を記載すること
 - 着手前に何を前提知識として読むべきか
 - 何から始めるか
 - どんな手順で進めれば良いのか
 - 作業を進めるうえでの注意点
 - オーナーに注意深く言われたこと

## ■ 議事メモ（経緯）
- 本セッションにおける作業記録を詳細にメモとして残すセクション
- # このセッションでやったことが漏れなく、実施内容が「なぜそうしたのか、なぜそうなったのか」詳細に記載されていること
- # 決定済み事項が漏れなく、実施内容が「なぜそうしたのか、なぜそうなったのか」詳細に記載されていること
- # 未決事項が漏れなく、実施内容が「なぜそうしたのか、なぜそうなったのか」詳細に記載されていること
- オーナーからどんなFB・指摘があったのか、なぜそれが起きてしまったのか詳細がわかる形で記載されていること
- **タスク完了**なら「次の一歩」を「完了」と明記し、経緯（何を始め→何が分かった）が辿れるか確認する。
- **継続中**なら「次の一歩＋理由」を一読で分かるよう具体的に書く（何からか・なぜそこからかの両方）。
- TASKSに起票されたタスクの背景、目的、実施する内容を詳細に記載すること

本手順が実施できない場合：オーナーへ確認する

→ レビューとして、下記がすべてpassになることを確認する
□ 議事メモ(sessionlog)を基本形（ヘッダ→本体→議事メモ・関連ファイル節を含む）で作成されていること
□ 命名規則`yyyyMMddhhmm_タイトル_sessionlog.md`（時刻は`date`取得・命名不変）に従って作成されていること
□ 格納先`.company/secretary/work/task_logs/YYYY-MM/`（YYYY-MM＝ファイル名先頭yyyyMM・規約正本=本スキル②／配置先の地図=DIRECTORY_TREE.md §1）が指定された場所以外になっていないこと
□ 実施していないのに「完了」としていないこと

　failした場合：直ちに修正し、再度レビューを実施する
　全passするまで③へ進まない

### ③ タスク表の更新

TASKS.mdを触る際は、TASKS.mdに記載されている**記法ルール**を厳守すること

- **タスク完了時**：
  - **該当タスクを TASKS_COMPLETED.md の末尾に追記**（`- [x] **タスク名** | 完了日・内容サマリ`）
  - **TASKS.md から該当タスクを即削除**（`[x]` をTASKS.mdに残さない）。完了項目の本文に残作業があれば、それを TASKS.md の独立 `[ ]` として切り出してから削除する。
- **継続時**：TASKS.md を最新状態へ更新する。

本手順が実施できない場合：オーナーへ確認する

→ レビューとして、下記がすべてpassになることを確認する
□ 未完了のタスクが、TASKS表へ更新されていること
□ 完了したタスクが、TASKS_COMPLETED.mdへ更新されていること
□ TASKS.mdに記載されている**記法ルール**に従って更新されていること
□ 実施していないのに「完了」としていないこと

　failした場合：直ちに修正し、再度レビューを実施する
　全passするまで④へ進まない

### ④ メディア中間物の棚卸し（タスクでメディアを生成/DLした場合）

- このタスクで生成・DLした画像/動画/音声を洗い出す。**置き場は`DIRECTORY_TREE.md`を参照する
- 洗い出したデータの中から、下記に当てはまるデータを`rm` でなく**ゴミ箱へ移動**すること
　- タスクが完了したため、そのデータはもう何からも参照されないものである
　- オーナーに目視確認をしてもらうためだけに作った一時生成物である
　- 候補として取得したが結果的に採用されず、今後使用する予定のないデータである
　- オーナーにゴミ箱移動を指示されたデータである
　- 同じデータとしての重複が確認できたデータである

→ レビューとして、下記がすべてpassになることを確認する
□ 削除に迷った場合、勝手に判断してゴミ箱へ移動した件数がゼロ件であること
□ 削除対象データに対して`rm` でなく**ゴミ箱へ移動**の対応を行った
□ 実施していないのに「完了」としていないこと

　failした場合：直ちに修正し、再度レビューを実施する
　全passするまで⑤へ進まない

### ⑤ 関連台帳の更新

- このタスクで `.github/workflows/*.yml` または稼働スクリプトを**追加・変更・無効化**したら：
  - 新規WF/スクリプトなら `ops/inventory_meta.yml` に**目的を1行追加**（ステータス/cronは自動導出なので書かない）。
  - **`python ops/wf_inventory.py generate`** で `WORKFLOWS.md` / `SCRIPTS.md` を再生成（実体から最新化）。
  - `python ops/wf_inventory.py check` がドリフトなし（exit 0）を確認（足し忘れ・再生成漏れはここで弾く・preventiveも毎日検知）。
- 台帳は**現状の真実源**＝診断/提案はまず `WORKFLOWS.md` を読む（他タスクのlogは読みに行かない）[[feedback_diagnose_failure_mechanism_from_logs]]。
- .company/ 配下【組織定義】(誰の成果物でもない共通基盤)に格納されている台帳関連を把握する
- このセッションにおける決定事項が関連する台帳があれば、それぞれの台帳へ反映する

→ レビューとして、下記がすべてpassになることを確認する
□ このセッションで記録された内容と、各台帳に記載された内容・ステータスが一致していること
□ 実施していないのに「完了」としていないこと

　failした場合：直ちに修正し、再度レビューを実施する
　全passするまで⑥へ進まない

### ⑥ `DIRECTORY_TREE.md`（root直下＝配置の地図）の更新

- このセッションで**新しいトップレベル/用途フォルダを新設・廃止（増減）した**なら、`DIRECTORY_TREE.md`（root直下＝配置の地図）の該当注記を**追記/削除**して地図と現物を一致させる。

→ レビューとして、下記がすべてpassになることを確認する
□ このセクションで起きたディレクトリに関するアクションと、`DIRECTORY_TREE.md`に記載されている内容が一致していること
□ 実施していないのに「完了」としていないこと

　failした場合：直ちに修正し、再度レビューを実施する
　全passするまで⑦へ進まない

### 　⑦ 選択的コミット & プッシュ & mainマージ（両リポジトリ・安全策つき）

- まず `git -C <repo> status --short` で作業ツリーを確認する。
- **本まとめ作業に関係する変更だけをパス指定で `git add`** する（例：更新した task_log・TASKS.md・TASKS_COMPLETED.md・①で昇華した `fb/<task>.md`・`pm/review-baseline.md`・`knowledge/kb-*.md`・該当skillSKILL.md）。
- ⚠️ **`git add -A`・`git add .` は禁止**。**他タスクの進行中の編集途中ファイル・未追跡物（`drafts/` 等）は巻き込まない**。
- auto-pushは可（バックアップ済み＝復元可能のため自走OK）。両リポジトリそれぞれで commit & push する。
- **push後は必ず `gh pr create --base main` でPRを作り、コンフリクトなくマージ可能なら `gh pr merge` まで行う。
- mainへのマージ完了をもってタスク完了とする**（ブランチにpushしただけは未完了＝ブランチ滞在中はmainと乖離が育つ）。
- mainは自動化botが毎日進めるため、**溜めずに都度マージする**（ブランチが長生きするほど衝突・乖離が増える）。
- **マージできない事情（衝突・レビュー待ち・GO待ち等）があるときのみ**、`gh pr merge` せず **PR URL と理由をオーナーへ報告して閉じる**（マージは次アクション）。
- ⚠️ **関係/無関係の切り分けが曖昧な場合、または他作業の未コミット変更が混在して判断に迷う場合は、push せずに、対処案と判断材料（メリデメ・リスク）を添えてオーナーへ確認する**。
- ※auto-memory（`~/.claude/projects/.../memory/`）への昇華はgit対象外（自動永続）。コミットするのはリポジトリ内の変更のみ。

→ レビューとして、下記がすべてpassになることを確認する
□ status --short を確認し、関係する変更だけをパス指定で add したか
□ `git add -A`・`git add .` を使っていないか
□ 他タスクの編集途中ファイル・未追跡物（drafts/等）を巻き込んでいないか
□ 切り分けが曖昧／他作業の未コミット変更が混在する場合、push せずオーナーへ確認したか
□ push後に `gh pr create --base main` でPRを作り、マージ可能なら `gh pr merge` まで行い**mainマージ完了＝タスク完了**としたか（マージできない事情があるときのみPR URLと理由を報告して閉じたか）
□ 実施していないのに「完了」としていないこと

　failした場合：直ちに修正し、再度レビューを実施する
　全passするまで⑧へ進まない

### ⑧ 実施内容をオーナーへ報告する

継続中（未完了）のときの追加案内：

- 〔タスク名〕〔該当ファイル〕は、**実際にそのまとめ作業で扱ったタスク名・task_logファイル名に置き換えて出力する**（プレースホルダのまま出さない）

出力フォーマット（例）

```
▶ 作業を再開するとき：秘書にこう伝えてください
「〔タスク名〕の続きをやって」（または「〔タスク名〕を再開して」）
→ 秘書が TASKS.md と最新議事メモ `.company/secretary/work/task_logs/YYYY-MM/〔該当sessionlogファイル〕.md` を全文読んで続きから着手します。
```

タスクが「完了／クローズ」のとき：

完了報告のみ

